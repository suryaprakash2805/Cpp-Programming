#include<iostream>
using namespace std;
int sum(int num)
{
    if(num<=1)
    {
        return num;
    }
    return num+sum(num-1);
}
int main()
{
 int num;
 cin>>num;
 cout<<"Sum of n numbers:"<<endl;
 cout<<sum(num);
}
